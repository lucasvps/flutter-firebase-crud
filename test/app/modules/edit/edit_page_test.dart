import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:todo_firebase/app/modules/edit/edit_page.dart';

main() {
  testWidgets('EditPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EditPage(title: 'Edit')));
    //  final titleFinder = find.text('Edit');
    //  expect(titleFinder, findsOneWidget);
  });
}
