import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('SegundaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SegundaPage(title: 'Segunda')));
    //  final titleFinder = find.text('Segunda');
    //  expect(titleFinder, findsOneWidget);
  });
}
